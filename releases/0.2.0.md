# 0.2.0

Adds private signed product dispatch, protected runtime installation, crash recovery, independent expiry, bounded HTTP/WebSockets, retained diagnostics and QA findings. Completes signed in-place renewal, unprivileged bounded Chromium checks and the offline guest-image build pipeline. BrowserProbe is a new project in this repository. Exact-image hardened Hyper-V acceptance and release signing remain deployment gates.
